\documentclass[tikz,border=4pt]{standalone}
\usepackage{amsmath}
\usetikzlibrary{arrows.meta,calc}

\begin{document}
\pagecolor{white}
\begin{tikzpicture}[
    >=Latex,
    line cap=round,
    line join=round,
    font=\sffamily,
    note/.style={draw=black!60, rounded corners=1.5pt, fill=white,
                 align=center, inner xsep=6pt, inner ysep=4pt,
                 font=\sffamily\small},
    flow/.style={blue!78!black, -{Latex[length=3.6mm,width=2.2mm]}, line width=0.9pt},
    wall/.style={black!82, line width=28pt},
    fluid/.style={blue!6, line width=22pt},
    bc/.style={red!75!black, line width=1.4pt},
    profile/.style={black!55, dashed, line width=0.95pt},
    dim/.style={<->, black!70, line width=0.55pt}
]

\def\R{1.75}
\def\a{0.34}
\def\cx{2.25}
\def\cy{2.75}
\def\xout{6.55}

\coordinate (inletCenter) at ({\cx-\R},0.35);
\coordinate (bendStart) at ({\cx-\R},\cy);
\coordinate (bendEnd) at (\cx,{\cy+\R});
\coordinate (outletCenter) at (\xout,{\cy+\R});
\coordinate (bendCenter) at (\cx,\cy);

% Pipe drawn as a stroked centerline, keeping the flow arrows away from walls.
\draw[wall] (inletCenter) -- (bendStart)
    arc[start angle=180,end angle=90,radius=\R] -- (outletCenter);
\draw[fluid] (inletCenter) -- (bendStart)
    arc[start angle=180,end angle=90,radius=\R] -- (outletCenter);

% Boundary planes.
\draw[bc] ({\cx-\R-\a},0.35) -- ({\cx-\R+\a},0.35);
\draw[bc] (\xout,{\cy+\R-\a}) -- (\xout,{\cy+\R+\a});

% Flow direction arrows on the centerline.
\draw[flow] ({\cx-\R},0.78) -- ({\cx-\R},1.45);
\draw[flow] ({\cx-\R},1.80) -- ({\cx-\R},2.45);
\draw[flow] ({\cx+\R*cos(158)},{\cy+\R*sin(158)})
    arc[start angle=158,end angle=112,radius=\R];
\draw[flow] (2.75,{\cy+\R}) -- (3.55,{\cy+\R});
\draw[flow] (4.35,{\cy+\R}) -- (5.15,{\cy+\R});

% 75 degree profile plane measured from the bend inlet.
\pgfmathsetmacro{\theta}{105}
\coordinate (profInner) at ({\cx+(\R-\a)*cos(\theta)},{\cy+(\R-\a)*sin(\theta)});
\coordinate (profOuter) at ({\cx+(\R+\a)*cos(\theta)},{\cy+(\R+\a)*sin(\theta)});
\coordinate (profMid) at ({\cx+\R*cos(\theta)},{\cy+\R*sin(\theta)});
\draw[profile] (profInner) -- (profOuter);

% Radius and diameter annotations.
\fill[black] (bendCenter) circle (1.05pt);
\draw[dim] (bendCenter) -- node[above, sloped, fill=white, inner sep=1pt]
    {$R_c=2.8D$} ($(bendCenter)!0.72!(profMid)$);
\draw[dim] ({\cx-\R-\a},0.72) -- node[above, fill=white, inner sep=1pt]
    {$D=1~\mathrm{m}$} ({\cx-\R+\a},0.72);

% Labels are kept outside the flow region to avoid overlaps.
\node[note, anchor=east, text width=35mm] at (-0.35,0.70)
    {fully developed\\turbulent inlet profile\\zero transverse velocity};
\node[note, anchor=south, text width=26mm] at (\xout,5.55)
    {pressure outlet\\$p=0~\mathrm{Pa}$};
\node[note, anchor=west, text width=32mm] (profileLabel) at (4.35,3.55)
    {$75^\circ$ along bend\\velocity-profile plane};
\draw[-{Latex[length=2.4mm,width=1.6mm]}, black!55, line width=0.65pt]
    (profileLabel.west) -- ($(profMid)+(0.08,0.02)$);

\node[note, anchor=north, text width=40mm] at (2.90,-0.05)
    {shown symmetry plane\\3D half-domain simulation\\$Re_D\approx43{,}000$};
\node[font=\sffamily\bfseries\large, anchor=west] at (-0.35,5.45)
    {VMFL030: 3D $90^\circ$ pipe bend};
\node[font=\sffamily\small, anchor=west, text=black!70] at (-0.35,5.15)
    {flow arrows indicate the pipe centerline direction};

\end{tikzpicture}
\end{document}
