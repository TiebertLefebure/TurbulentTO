\chapter{Introduction}
\label{cha:intro}

\section{Background and Motivation}
Many thermal and energy systems first face a hydraulic design problem: the working fluid has to be routed through a small available volume without wasting too much pumping power. This issue appears in liquid-cooled electronics, battery and power-electronics modules, fuel-cell and electrolyzer channels, compact heat exchangers, and coolant distribution hardware for automotive and aerospace systems. Even when a geometry offers good local heat-transfer potential, it can be unusable if the flow path creates a large pressure drop, poor distribution, or separated regions that undermine the intended operating point.

The broader motivation is thermal-management design, but the model problem treated here is intentionally hydraulic. The thesis does not solve the energy equation, does not perform conjugate heat-transfer optimization, and does not minimize temperature. It instead considers flow-only bends, U-bends, and diffusers, where the relevant behavior is pressure loss, viscous dissipation, separation, and the response of the turbulence closure. Establishing that hydraulic baseline is necessary before the same optimization framework can be trusted for turbulent heat-transfer problems.

Topology optimization is attractive for this setting because the designer does not prescribe the final channel layout beforehand. The method has become an established computational design approach for fluid and thermal-management applications \cite{Fawaz2022}. For a coupled thermal device, an objective could involve heat-transfer rate, maximum temperature, or thermal resistance. In the present flow-only setting, the objective is instead tied to pressure drop or dissipated mechanical energy. This scope reduction still leaves a difficult problem: in turbulent density-based topology optimization, the flow solver, wall treatment, turbulence model, geometry representation, and adjoint sensitivity calculation all change together as the density field evolves.

\begin{figure}[!ht]
	\centering
	\includegraphics[width=\textwidth]{Heat-Sink-TO}
	\caption{Topology-optimized heat sink from Barroca et al.~\cite{Barroca2023}.}
	\label{fig:barroca_heat_sink}
\end{figure}

The heat-sink example in Figure~\ref{fig:barroca_heat_sink} illustrates the type of hydraulic layout that motivates the present work: the optimized fin structure guides the velocity field while the thermal field responds to that flow distribution. The central gap addressed in this thesis is therefore that density-based fluid topology optimization is far more mature for laminar flows than for turbulent internal flows. Laminar assumptions are attractive because they simplify both the governing equations and the associated adjoint sensitivities, but they are often not representative of compact engineering flow paths operating at industrial Reynolds numbers. In turbulent internal flows, the chosen Reynolds-Averaged Navier-Stokes (RANS) closure, near-wall treatment, and response to curvature or separation can influence the optimized topology itself. Before turbulent conjugate heat-transfer optimization can be treated as a reliable design tool, the underlying turbulent flow-only optimization problem must first be stabilized, verified, validated, and interpreted critically.

\section{Conceptual Overview of Density-Based Topology Optimization}
The distinction between topology optimization and more classical design optimization is important for the turbulent-flow problem. Size optimization changes parameters such as member thicknesses, while shape optimization moves the boundary of an already chosen layout. Both can be effective, but both inherit the connectivity of the initial design. A shape optimizer, for example, can refine an existing bend but cannot freely decide to split one passage into two or create a new opening.

Density-based topology optimization, pioneered by Bendsoe and Kikuchi in 1988 for solid mechanics \cite{Bendsoe1988}, approaches the problem differently. Instead of moving boundaries, the design space is discretized into a fixed grid of finite elements. The algorithm then determines the optimal distribution of material across this grid. In fluid problems, this is achieved by assigning a continuous design variable, $\gamma \in [0, 1]$, to every element in the domain. In the convention adopted for this thesis, $\gamma = 1$ represents a pure fluid channel, while $\gamma = 0$ represents a solid, impermeable structure.

For gradient-based optimization, the binary fluid--solid choice is relaxed to a continuous field. Intermediate values are then interpreted through a porous-medium model: the momentum equation receives a Brinkman-type resistance whose magnitude depends on the local density \cite{Borrvall2003,Gersborg2005}. The optimizer updates this field to reduce a chosen objective, such as pressure loss or dissipated power, while satisfying constraints such as a prescribed fluid volume fraction. Figure~\ref{fig:to_iterative_process} illustrates the typical path from an initially uniform field, through a smooth intermediate design, toward a nearly discrete topology. In this introductory figure, the displayed quantity should be read as the physical projected density used by the flow equations; Chapter~\ref{ch:topology_optimization_methodology} later separates this from the raw design variable and the filtered density.

\begin{figure}[htpb]
	\centering
	\begin{subfigure}[t]{0.28\textwidth}
		\centering
		\vspace{0pt}
		\includegraphics[height=0.13\textheight,keepaspectratio]{Initial-Density-Field}
		\caption{Initial uniform density}
		\label{fig:to_initial}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.28\textwidth}
		\centering
		\vspace{0pt}
		\includegraphics[height=0.13\textheight,keepaspectratio]{Intermediate-Density-Field}
		\caption{Continuous distribution}
		\label{fig:to_intermediate}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.38\textwidth}
		\centering
		\vspace{0pt}
		\includegraphics[height=0.13\textheight,keepaspectratio]{Final-Density-Field}
		\caption{Discrete topology}
		\label{fig:to_final}
	\end{subfigure}
	\caption{Density-field evolution during topology optimization.}
	\label{fig:to_iterative_process}
\end{figure}

\section{From Laminar to Turbulent Fluid Topology Optimization}
While density-based topology optimization became a standard tool in structural engineering in the late 1990s, its application to fluid dynamics is relatively recent. The optimization of internal-flow layouts remains an active and highly challenging area of research \cite{Fawaz2022}. The genesis of fluid topology optimization is widely attributed to Borrvall and Petersson (2003), who successfully applied the technique to minimize power dissipation in creeping Stokes flow \cite{Borrvall2003}. This was quickly followed by Gersborg-Hansen et al. (2005), who extended the framework to steady, laminar Navier-Stokes flows \cite{Gersborg2005}. Over the past decade, the literature surrounding laminar flow optimization expanded extensively, adapting the methodology to standard incompressible Navier-Stokes equations \cite{Deng2013, Alexandersen2020, Zhou2017}.

However, the assumption of laminar flow significantly simplifies both the governing equations and the derivation of adjoint sensitivities required for gradient-based optimization. Unfortunately, this assumption prevents the application of these methodologies to many industrial and high-performance applications, where flow velocities are high and turbulence dominates the flow physics. Only recently has the computational mechanics community begun to tackle the severe non-linearities and numerical instabilities associated with optimizing geometries directly in the turbulent regime \cite{Yoon2016, Dilgen2018, Zhao2021}.

Extending topology optimization to the turbulent regime is highly non-trivial. It requires coupling optimization algorithms with Reynolds-Averaged Navier-Stokes (RANS) turbulence models. These models introduce steep velocity gradients, highly non-linear source terms, near-wall resolution requirements, and severe numerical instabilities during the iterative optimization process. To mitigate this, gradient-based turbulent optimizations frequently rely on the ``frozen turbulence'' assumption, where the turbulent viscosity field is updated in the forward solve but treated as fixed during sensitivity assembly. While this assumption provides necessary solver stability and has driven success in industrial adjoint shape optimization for automotive aerodynamics \cite{Othmer2014}, advancing toward fully coupled turbulent sensitivities in topology optimization remains an ongoing mathematical challenge.

\begin{figure}[htpb]
	\centering
	\begin{minipage}[c]{0.37\textwidth}
		\includegraphics[width=\textwidth]{Manifold-TO-domain}
	\end{minipage}
	\hfill
	\begin{minipage}[c]{0.60\textwidth}
		\includegraphics[width=\textwidth]{Manifold-TO-solution}
	\end{minipage}
		\caption{Turbulent-flow topology optimization example. Adapted from Dilgen et al.~\cite{Dilgen2018}.}
	\label{fig:dilgen_composite}
\end{figure}

Pioneering works by Yoon (2016) \cite{Yoon2016} and Dilgen et al. (2018) \cite{Dilgen2018} have pushed the boundaries of turbulent frameworks. As seen in Figure~\ref{fig:dilgen_composite}, integrating turbulence models allows the optimizer to generate complex internal-flow layouts tailored to guide turbulent flow distributions efficiently.

\section{Objectives and Research Questions}
The objective of this Master's thesis is to build and assess a density-based topology-optimization framework for turbulent internal-flow configurations. The title deliberately uses ``flow configurations'' because the work addresses the hydraulic part of energy and thermal-management devices rather than the complete heat-transfer device. The framework combines incompressible flow, RANS turbulence modeling, Brinkman topology penalization, and gradient-based optimization in a finite-element implementation. By separating the fluid mechanics from heat transfer, the thesis focuses on a baseline that can be verified and later extended.

The core implementation is written in \textbf{FEniCS}, an open-source finite-element platform for partial differential equations \cite{Alnaes2015}. This choice keeps the weak forms and residual derivatives explicit, which is useful for implementing the Spalart-Allmaras (SA) turbulence equation and for assembling the frozen-turbulence adjoint used later in the optimization loop.

At this stage, it is important to distinguish two different comparisons that appear later in the thesis. A comparison between laminar-optimized and turbulent-optimized layouts is useful as supporting design context, because it shows how inertia and turbulence alter the preferred topology. However, this comparison is not treated as the principal CFD validation step. The decisive assessment instead consists of validating the final SA-optimized geometries in Ansys Fluent, first with the same SA model and then with the SST $k-\omega$ closure.

The work is organized around three research questions:
\begin{enumerate}
	\item \textbf{How accurately does the Spalart-Allmaras turbulence model predict curved and separated internal turbulent flows, and where do its physical limitations appear?}

	Chapter~\ref{ch:turbulence_modeling} answers this question by combining turbulence-model theory, curved-pipe and separated-flow literature evidence, and Ansys Fluent comparisons on curved internal-flow benchmarks. The Dean number is used only as a rough physical scale for the bend cases, not as a universal validity threshold for SA.

	\item \textbf{Does the custom FEniCS implementation of the Spalart-Allmaras model reproduce Ansys Fluent predictions for the selected turbulent internal-flow benchmarks?}

	Chapters~\ref{ch:fenics_implementation} and~\ref{ch:sa_verification} answer this question by presenting the weak forms, implementing the standalone SA solver, and comparing it against Ansys Fluent on a turbulent channel and a U-bend benchmark.

	\item \textbf{To what extent do SA-driven topology optimizations produce final designs whose predicted performance remains consistent under independent Ansys Fluent validation with SA and SST $k-\omega$, and how do they compare with laminar-optimized designs under identical turbulent operating conditions?}

	Chapter~\ref{ch:benchmark_results} answers this question using the pipe-bend, U-bend, and diffuser benchmarks. It reports the FEniCS optimizations, sharp-geometry Ansys-SA validation, SST $k-\omega$ validation, and laminar-versus-turbulent design comparisons. Appendix~\ref{app:C_topology_optimization_benchmarks} contains the supporting frozen-sensitivity verification.
\end{enumerate}

\section{Outline of the Thesis}
The remainder of this thesis is structured into the following six chapters:
\begin{itemize}
	\item \textbf{Chapter~\ref{ch:turbulence_modeling} (Turbulence Modeling):} Provides the theoretical background on fluid dynamics and turbulence. It discusses the Navier-Stokes equations, RANS modeling, near-wall treatment, the physical characteristics of the Spalart-Allmaras model, and the curvature and separation limits relevant to this thesis.
	\item \textbf{Chapter~\ref{ch:fenics_implementation} (Implementation of the Spalart-Allmaras Model in FEniCS):} Details the numerical implementation of the standalone SA solver in FEniCS, including weak forms, IPCS flow solves, Picard coupling, and wall-distance computation.
	\item \textbf{Chapter~\ref{ch:sa_verification} (Verification of the Spalart-Allmaras Solver for Turbulent Internal Flows):} Verifies the standalone FEniCS-SA forward solver against Ansys Fluent on turbulent channel-flow and U-bend benchmarks.
	\item \textbf{Chapter~\ref{ch:topology_optimization_methodology} (Turbulent Topology Optimization Methodology):} Introduces the density-based optimization framework. It details Brinkman momentum penalization, topology penalties, reciprocal wall-distance penalization, filtering, projection, MMA, and the frozen-turbulence adjoint.
	\item \textbf{Chapter~\ref{ch:benchmark_results} (Turbulent Topology Optimization Results):} Presents the turbulent topology optimization results for the pipe-bend and U-bend benchmarks from Bayat et al.~\cite{Bayat2026} and the diffuser benchmark from Yoon~\cite{Yoon2016}, and evaluates extracted designs with both SA and SST $k-\omega$ turbulence models. Appendix~\ref{app:C_topology_optimization_benchmarks} documents the pipe-bend sensitivity verification and numerical support material.
	\item \textbf{Chapter~\ref{ch:conclusion} (Conclusion and Future Work):} Summarizes the main findings, answers the research questions conservatively, and outlines remaining limitations and future extensions toward three-dimensional and conjugate heat-transfer applications.
\end{itemize}
