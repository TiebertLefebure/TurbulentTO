\chapter{Turbulent Topology Optimization Results}
\label{ch:benchmark_results}

\providecommand{\validationplaceholder}[1]{%
	\fbox{\parbox[c][0.22\textheight][c]{0.88\linewidth}{\centering \textbf{Placeholder}\\[0.5em]#1}}%
}

\section{Introduction}
\label{sec:ch6_introduction}
Building upon the FEniCS-SA solver verified in Chapter~\ref{ch:sa_verification} and the topology optimization methodology developed in Chapter~\ref{ch:topology_optimization_methodology}, this chapter evaluates the turbulent topology optimization solver on the pipe-bend and U-bend internal-flow benchmarks from Bayat et al.~\cite{Bayat2026}. Both benchmarks use the final thesis formulation: reciprocal wall-distance penalization, topology-dependent SA and wall-distance reaction terms, an average inlet-pressure objective, and the frozen-turbulence adjoint.

The chapter first verifies the implemented frozen sensitivity on the same pipe-bend configuration used for optimization with a Dilgen-style coordinate finite-difference check on selected design cells and a directional Taylor check. The optimized pipe-bend and U-bend layouts are then interpreted through their projected density fields, velocity fields, convergence histories, and independent sharp-geometry CFD validation. This ordering keeps the derivative evidence close to the optimization result that uses it, while leaving the standalone forward-flow verification in Chapter~\ref{ch:sa_verification}.

The U-bend serves as a companion topology optimization benchmark with the same governing assumptions and objective structure, but with a more severe $180^\circ$ flow reversal. Detailed pipe-bend geometry and FEniCS settings are listed in Tables~\ref{tab:app_B_alexandersen_pipebend_physical} and \ref{tab:app_B_alexandersen_pipebend_numerical} of Section~\ref{sec:app_B_config_alexandersen_pipebend}; the corresponding U-bend setup is listed in Tables~\ref{tab:app_B_alexandersen_ubend_physical} and \ref{tab:app_B_alexandersen_ubend_numerical} of Section~\ref{sec:app_B_config_alexandersen_ubend}. The Ansys grid-independence studies for the extracted turbulent designs are reported in Tables~\ref{tab:app_B_alexandersen_pipebend_mesh} and \ref{tab:app_B_alexandersen_ubend_mesh}. Laminar-versus-turbulent comparisons are retained in Section~\ref{sec:app_C_alexandersen_pipebend} and Section~\ref{sec:app_C_alexandersen_ubend} of Appendix~\ref{app:C_laminar_turbulent_topology_comparison} as supporting evidence for interpreting the optimized layouts.

\section{Benchmark Strategy and Evaluation Metrics}
\label{sec:ch6_strategy_metrics}

Table~\ref{tab:ch6_benchmark_strategy} outlines the strategic role of the two benchmarks. The pipe bend receives the finite-difference sensitivity verification because it is the first final-configuration optimization case. The U-bend is then used to test whether the same methodology remains robust for a longer turning path with stronger flow reversal.

\begin{table}[!ht]
	\centering
	\footnotesize
	\caption{Benchmark strategy used in Chapter~\ref{ch:benchmark_results}. Detailed physical and numerical parameters are listed in Sections~\ref{sec:app_B_config_alexandersen_pipebend} and \ref{sec:app_B_config_alexandersen_ubend} of Appendix~\ref{app:B_validation_configurations}.}
		\label{tab:ch6_benchmark_strategy}
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\textwidth}{l X X X}
		\hline
			\textbf{Case} & \textbf{Role in this thesis} & \textbf{Reference comparison} & \textbf{Validation scope} \\
			\hline
			Pipe bend & Primary verification and optimization case at $V_f=0.25$ & Pipe-bend setup and optimized fields from Bayat et al.~\cite{Bayat2026} & Coordinate finite-difference and Taylor checks; FEniCS optimization; Ansys-SA and Ansys-SST validation; laminar-design comparison \\
			U-bend & Companion curved-flow topology case at $V_f=0.27$ & U-bend setup and optimized fields from Bayat et al.~\cite{Bayat2026} & FEniCS optimization; Ansys-SA and Ansys-SST validation; laminar-design comparison \\
			\hline
		\end{tabularx}
	\end{table}

\subsection{Optimization and Validation Metrics}
\label{subsec:ch6_metrics}
Throughout the optimization process, both benchmarks minimize the average inlet-pressure objective $J_p$ defined in Eq.~\ref{eq:inlet_pressure_objective}. This pressure is averaged over the physical inlet boundary of the fixed non-design extension, where the inlet velocity is prescribed. The outlet pressure is fixed to zero, so reducing $J_p$ directly reduces the pressure level required to drive the prescribed inlet flow through the evolving topology. The optimization problem is still solved on a diffuse Brinkman domain, but once a design is extracted for Ansys Fluent, the Brinkman penalty is removed and the solid-fluid interface becomes a sharp wall.

Bayat et al.~\cite{Bayat2026} report corresponding final average inlet-pressure objectives for their standard $k$-$\varepsilon$ computations in Table~7. Their ``conventional'' method is the density-based turbulent topology optimization formulation without the proposed implicit wall-functions: solid regions are imposed through Brinkman/body-force penalization, including damping of $k$ and $\varepsilon$ toward zero in solid material, on the same diffuse optimization mesh. It is therefore distinct from the body-fitted re-simulations with explicit wall-functions that they use for validation. Since the present thesis uses a wall-resolved SA model instead of $k$-$\varepsilon$, the conventional values from Bayat et al. are used only as scale comparisons in the individual benchmark discussions below.

To bridge this gap, the primary validation metric is the physical viscous dissipation integrated over the full exported CFD domain, including the non-design inlet, outlet, and fixed regions that are retained in the final geometry,
\begin{equation}
	\Phi_D = \int_{\Omega}
	\frac{1}{2} \mu_{\mathrm{eff}}
	\left(\nabla\mathbf{u}+\nabla\mathbf{u}^{T}\right):
	\left(\nabla\mathbf{u}+\nabla\mathbf{u}^{T}\right)
	\,d\Omega ,
	\label{eq:ch6_validation_dissipation}
\end{equation}
where $\Omega$ denotes the complete sharp-geometry flow domain exported to Ansys. The effective viscosity is $\mu_{\mathrm{eff}}=\mu+\mu_t$ for turbulent evaluations and $\mu_{\mathrm{eff}}=\mu$ for laminar reference solves. Furthermore, because pressure drop is the most direct and practical engineering interpretation of these flow losses, $\Delta p$ is also reported from the Ansys validation runs. All two-dimensional dissipation quantities are evaluated per unit depth.

\subsection{Wall-Distance and SA Penalization Conventions}
\label{subsec:ch6_wall_distance_note}
The two benchmark computations use one wall-distance and penalty convention throughout: the reciprocal wall-distance equation is penalized by the power term in Eq.~\ref{eq:penalized_eikonal}, and the SA working variable is suppressed by the power sink in Eq.~\ref{eq:penalized_sa}. The wall-distance field is updated in the forward loop, but it is treated as a frozen coefficient during the adjoint sensitivity solve. Thus, the sensitivity verification below checks the implemented frozen derivative for the same practical assumption used by the optimizer.

\section{Pipe Bend Benchmark}
\label{sec:alexandersen_pipebend}
The pipe-bend benchmark introduces a direct comparison with the turbulent topology optimization results of Bayat et al.~\cite{Bayat2026}. While the reference study employs a standard $k$-$\varepsilon$ RANS model with wall functions, the present framework tackles the same problem using a wall-resolved SA model under the frozen-turbulence assumption. This comparison tests whether the fundamental topological solution, a streamlined and separation-avoiding bend, remains consistent across different turbulence closures.

\subsection{Geometry and Boundary Conditions}
\label{subsec:alexandersen_pipebend_setup}
The boundary conditions and geometric constraints replicate the setup shown in Fig.~10 of Bayat et al.~\cite{Bayat2026}, specifically targeting the $V_f=0.25$ volume-fraction case. The inlet is placed on the left side of the non-design extension, the outlet is placed along the lower boundary, and the outlet pressure is fixed to zero. The optimization minimizes the average pressure on this prescribed inlet boundary required to drive the flow through the evolving bend.
The local benchmark geometry used for the present implementation is shown in Figure~\ref{fig:alexandersen_pipebend_setup}.

\begin{figure}[!ht]
	\centering
	\includegraphics[width=0.8\textwidth]{Alexandersen-PipeBend-GeometryBCs}
	\caption{Geometry and boundary conditions for the pipe-bend benchmark from Bayat et al.~\cite{Bayat2026}. The present evaluation targets the $V_f=0.25$ case using the SA model.}
	\label{fig:alexandersen_pipebend_setup}
\end{figure}

\subsection{Sensitivity Verification}
\label{subsec:alexandersen_pipebend_sensitivity}
Before interpreting the optimized topology, the frozen adjoint is checked directly on the pipe-bend configuration. This is the relevant local test for the final thesis setup because it uses the same topology-dependent SA and wall-distance penalties, inlet-pressure objective, and frozen-turbulence adjoint used during the optimization. No full turbulence-adjoint or wall-distance-adjoint comparison is made; the purpose is to compare the derivative supplied to MMA with objective changes obtained by re-solving the perturbed velocity-pressure state.

The check is performed at the initial pipe-bend design state, with base objective
$J_0=1.8363$. In every perturbation, the SA working variable $\tilde{\nu}^{0}$ and reciprocal wall-distance field $G^{0}$ are held fixed. This matches the frozen-turbulence assumption used by the adjoint: the flow state is re-solved, but the turbulence and wall-distance updates are not differentiated.

Five active $DG_0$ design cells are sampled deterministically from seed 13. These sampled control volumes are labelled $CV_1$--$CV_5$ in Table~\ref{tab:alexandersen_pipebend_fd_adjoint_comparison}. For sampled cell $i$, the raw density vector $\boldsymbol{\gamma}$ is perturbed by $\pm\Delta h\,\mathbf{e}_i$, where $\mathbf{e}_i$ is the unit vector for that active design variable and $\Delta h=10^{-6}$. The central finite-difference derivative is
\begin{equation}
	S_i^{\mathrm{FD}}
	=
	\frac{
			J_p\!\left(\boldsymbol{\gamma}+\Delta h\,\mathbf{e}_i;
			\mathbf{u}^{+},p^{+},\tilde{\nu}^{0},G^{0}\right)
		-
			J_p\!\left(\boldsymbol{\gamma}-\Delta h\,\mathbf{e}_i;
			\mathbf{u}^{-},p^{-},\tilde{\nu}^{0},G^{0}\right)
	}{
		2\Delta h
	}.
	\label{eq:alexandersen_pipebend_fd_metric}
\end{equation}
The frozen-adjoint value for the same cell is denoted by $S_i^{\mathrm{fr}}$; the superscripts $\mathrm{FD}$ and $\mathrm{fr}$ denote finite-difference and frozen-adjoint quantities, respectively. The absolute and relative discrepancies are computed as
\begin{equation}
	\Delta_i^{\mathrm{abs}} =
	\left|S_i^{\mathrm{fr}}-S_i^{\mathrm{FD}}\right|,
	\qquad
	\varepsilon_i^{\mathrm{fr}}
	=
	\frac{\Delta_i^{\mathrm{abs}}}
	{\max\!\left(\left|S_i^{\mathrm{fr}}\right|,\left|S_i^{\mathrm{FD}}\right|,10^{-30}\right)} .
	\label{eq:alexandersen_pipebend_fd_relative_error}
\end{equation}

\begin{table}[!ht]
	\centering
	\scriptsize
	\caption{Coordinate sensitivity check for the frozen pipe-bend configuration. Values are taken from \texttt{SensitivityVerificationTable.tsv}.}
	\label{tab:alexandersen_pipebend_fd_adjoint_comparison}
	\renewcommand{\arraystretch}{1.2}
	\begin{tabular}{l c c c c}
		\hline
		\textbf{Cell} &
		\shortstack{\textbf{Finite}\\\textbf{difference}\\$S_i^{\mathrm{FD}}$} &
		\shortstack{\textbf{Frozen}\\\textbf{adjoint}\\$S_i^{\mathrm{fr}}$} &
		\shortstack{\textbf{Absolute}\\\textbf{error}\\$\Delta_i^{\mathrm{abs}}$} &
		\shortstack{\textbf{Relative}\\\textbf{error}\\$\varepsilon_i^{\mathrm{fr}}$} \\
		\hline
		$CV_1$ & $-2.7041\times10^{-5}$ & $-2.7042\times10^{-5}$ & $4.1371\times10^{-10}$ & $1.5299\times10^{-5}$ \\
		$CV_2$ & $-2.1494\times10^{-6}$ & $-2.1491\times10^{-6}$ & $3.1679\times10^{-10}$ & $1.4738\times10^{-4}$ \\
		$CV_3$ & $-1.6641\times10^{-4}$ & $-1.6641\times10^{-4}$ & $3.6170\times10^{-10}$ & $2.1735\times10^{-6}$ \\
		$CV_4$ & $-1.7281\times10^{-5}$ & $-1.7281\times10^{-5}$ & $1.5118\times10^{-12}$ & $8.7488\times10^{-8}$ \\
		$CV_5$ & $2.3808\times10^{-5}$ & $2.3808\times10^{-5}$ & $8.1272\times10^{-11}$ & $3.4136\times10^{-6}$ \\
		\hline
	\end{tabular}
\end{table}

The coordinate check shows close agreement between the frozen-adjoint derivative and the re-solved central finite differences. The largest absolute discrepancy is $4.14\times10^{-10}$, and the largest relative discrepancy is $1.47\times10^{-4}$ for $CV_2$. This relative error remains small and is amplified by the small magnitude of the $CV_2$ derivative, approximately $2.15\times10^{-6}$, which places the finite-difference objective change generated by a $10^{-6}$ perturbation close to roundoff.

\begin{table}[!ht]
	\centering
	\scriptsize
	\caption{Directional Taylor check for the frozen pipe-bend configuration. Values are taken from \texttt{TaylorCheckLog.tsv}. The same five sampled cells as in Table~\ref{tab:alexandersen_pipebend_fd_adjoint_comparison} are perturbed together.}
	\label{tab:alexandersen_pipebend_taylor_check}
	\renewcommand{\arraystretch}{1.2}
	\begin{tabular}{c c c c c c}
		\hline
		\textbf{Step} &
		\shortstack{\textbf{Adjoint}\\\textbf{directional}\\\textbf{derivative}} &
		\shortstack{\textbf{Central}\\\textbf{finite-difference}\\\textbf{directional}\\\textbf{derivative}} &
		\shortstack{\textbf{Relative}\\\textbf{error}} &
		\shortstack{\textbf{Symmetric}\\\textbf{Taylor}\\\textbf{remainder}} &
		\shortstack{\textbf{Observed}\\\textbf{order}} \\
		\hline
		$1.0\times10^{-3}$ & $9.6135\times10^{-5}$ & $9.6135\times10^{-5}$ & $1.5795\times10^{-8}$ & $2.1515\times10^{-11}$ & -- \\
		$3.0\times10^{-4}$ & $9.6135\times10^{-5}$ & $9.6135\times10^{-5}$ & $3.8379\times10^{-9}$ & $1.9358\times10^{-12}$ & $2.0003$ \\
		$1.0\times10^{-4}$ & $9.6135\times10^{-5}$ & $9.6135\times10^{-5}$ & $1.5386\times10^{-8}$ & $2.1494\times10^{-13}$ & $2.0006$ \\
		$3.0\times10^{-5}$ & $9.6135\times10^{-5}$ & $9.6135\times10^{-5}$ & $3.4657\times10^{-8}$ & $1.9096\times10^{-14}$ & $2.0107$ \\
		\hline
	\end{tabular}
\end{table}

The Taylor check provides a complementary directional test. Instead of perturbing one cell at a time, the test perturbs the same five sampled cells together along one fixed direction and checks whether the objective change follows the adjoint's first-order prediction as the step is reduced. The direction signs for $CV_1$--$CV_5$ are $(+,+,-,+,-)$, defining a combined perturbation of the coordinate-check cells rather than a separate set of locations. Table~\ref{tab:alexandersen_pipebend_taylor_check} shows that the central finite-difference directional derivatives agree with the adjoint value to a maximum relative error of $3.47\times10^{-8}$ over the tested steps. The Taylor remainders decrease with approximately second-order rates, which is the expected behavior when the implemented gradient captures the local first-order objective change.

These tests therefore verify the implemented frozen-turbulence derivative for the pipe-bend setup used in the optimization. The conclusion is deliberately limited to that derivative: the SA working variable and reciprocal wall-distance field are held fixed during the checks, matching the optimization adjoint; consequently, the result should not be interpreted as validation of a fully coupled turbulence sensitivity. Repeating the same diagnostic on the U-bend would test the same algebraic implementation path rather than a fundamentally different adjoint formulation. Since there is no independent reference field for this SA frozen derivative, a spatial $dJ_p/d\gamma$ contour is not used as a verification metric; the relevant evidence is the coordinate finite-difference agreement and the second-order Taylor behavior reported above.

\subsection{Optimization Results}
\label{subsec:alexandersen_pipebend_fenics}
Figure~\ref{fig:alexandersen_pipebend_fenics_results} illustrates the projected density field $\bar{\gamma}$ and the corresponding velocity magnitude. Despite the differing turbulence models, the FEniCS-SA topology is expected to follow the same broad routing strategy as the $k$-$\varepsilon$ reference: the primary high-speed path should bypass the sharpest corners to minimize momentum loss, while boundary-layer treatment can still affect the exact gray-scale density distribution near the walls.

\begin{figure}[!ht]
	\centering
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\includegraphics[width=\textwidth]{Alexandersen-PipeBend-Topology}
		\caption{Projected density field $\bar{\gamma}$.}
		\label{fig:alexandersen_pipebend_topology}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\includegraphics[width=\textwidth]{Alexandersen-PipeBend-Velocity}
		\caption{Velocity magnitude field $u$.}
		\label{fig:alexandersen_pipebend_velocity}
	\end{subfigure}
	\caption{FEniCS-SA optimization result for the pipe-bend benchmark at $V_f=0.25$. Final objective function value $J_p= [TBD]$.}
	\label{fig:alexandersen_pipebend_fenics_results}
\end{figure}

For the same $V_f=0.25$, $Re=5{,}000$ operating point, Bayat et al.~\cite{Bayat2026} report $J_p=0.4023$ for their conventional $k$-$\varepsilon$ topology optimization. This value is used only as a reference scale, not as a direct validation target, because the turbulence closure and near-wall treatment differ from the present wall-resolved SA formulation.

Figure~\ref{fig:alexandersen_pipebend_histories} reports the convergence behavior that accompanies the final topology, separating the inlet-pressure objective history in Figure~\ref{fig:alexandersen_pipebend_objective_history} from the enforced volume-fraction history in Figure~\ref{fig:alexandersen_pipebend_volume_history}.

\begin{figure}[!ht]
	\centering
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\validationplaceholder{Objective function vs. optimization iteration}
		\caption{Objective function $J$ vs. MMA iteration.}
		\label{fig:alexandersen_pipebend_objective_history}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\validationplaceholder{Volume constraint vs. optimization iteration}
		\caption{Volume fraction vs. MMA iteration.}
		\label{fig:alexandersen_pipebend_volume_history}
	\end{subfigure}
	\caption{Optimization convergence histories for the pipe-bend benchmark.}
	\label{fig:alexandersen_pipebend_histories}
\end{figure}

\subsection{CFD Validation}
\label{subsec:alexandersen_pipebend_cfd}
The structural robustness of the extracted pipe-bend topology is verified in Table~\ref{tab:alexandersen_pipebend_validation_metrics}. Because the SST $k$-$\omega$ model is notably more sensitive to adverse pressure gradients than SA, evaluating the extracted geometry under both closures serves as a rigorous stress test for the optimized shape.

\begin{table}[!ht]
	\centering
	\footnotesize
	\caption{CFD validation metrics for the extracted pipe-bend topology.}
	\label{tab:alexandersen_pipebend_validation_metrics}
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\textwidth}{l c c c X}
		\hline
		\textbf{Solver} & \textbf{Model} & \textbf{$\Phi_D$ [W/m]} & \textbf{$\Delta p$ [Pa]} & \textbf{Comment} \\
		\hline
		FEniCS diffuse design & SA & [TBD] & [TBD] & Optimization state \\
		Ansys sharp geometry & SA & [TBD] & [TBD] & Cross-code comparison \\
		Ansys sharp geometry & SST $k$-$\omega$ & [TBD] & [TBD] & Turbulence-model sensitivity \\
		\hline
	\end{tabularx}
\end{table}

\subsection{Comparison with Laminar Design}
\label{subsec:alexandersen_pipebend_laminar}
The laminar-design comparison is reported in Section~\ref{sec:app_C_alexandersen_pipebend} of Appendix~\ref{app:C_laminar_turbulent_topology_comparison}. The expected distinction is that a laminar bend optimized at $Re=1$ can minimize wall length and viscous shear in the creeping-flow limit, but may produce a sharper or less separation-aware turn when evaluated at the target turbulent Reynolds number. The comparison is therefore interpreted under identical turbulent operating conditions, rather than by comparing the laminar and turbulent optimization objectives directly.

\section{U-Bend Benchmark}
\label{sec:alexandersen_ubend}
The U-bend is the companion benchmark to the pipe bend. It uses the same frozen-SA topology optimization framework, but the geometry forces a $180^\circ$ return path between two ports on the same side of the domain. This makes the case useful for checking whether the optimizer can form a smooth turbulent return duct without relying on the shorter $90^\circ$ routing available in the pipe-bend case.

\subsection{Geometry and Boundary Conditions}
\label{subsec:alexandersen_ubend_setup}
The geometry and boundary conditions follow Fig.~11 and Table~4 of Bayat et al.~\cite{Bayat2026}. The square design region has side length $L$, with a non-design lead duct of length $0.2L$ on the left. The inlet and outlet ports both have height $0.2L$; the top port acts as the inlet and the lower port acts as the pressure outlet. A fixed internal separator bar of thickness $0.10L$ extends from the lead duct into the design domain, forcing the flow to turn around its rounded tip. The target volume fraction is $V_f=0.27$, and the Reynolds number is $Re=5{,}000$ based on the port height and maximum inlet velocity.
Figure~\ref{fig:alexandersen_ubend_setup} shows the corresponding local geometry and boundary-condition layout used for this thesis benchmark.

\begin{figure}[!ht]
	\centering
	\includegraphics[width=0.8\textwidth]{Alexandersen-UBend-GeometryBCs}
	\caption{Geometry and boundary conditions for the U-bend benchmark from Bayat et al.~\cite{Bayat2026}.}
	\label{fig:alexandersen_ubend_setup}
\end{figure}

\subsection{Optimization Results}
\label{subsec:alexandersen_ubend_fenics}
Figure~\ref{fig:alexandersen_ubend_fenics_results} reports the final projected density and velocity-magnitude field for the U-bend. The main feature to assess is whether the design forms a rounded return passage around the separator tip, rather than a short path with tight local curvature. Because the objective is average inlet pressure, a successful layout reduces the pressure required to maintain the imposed inlet flow while respecting the prescribed volume fraction.

\begin{figure}[!ht]
	\centering
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\validationplaceholder{Projected density field $\bar{\gamma}$, $V_f=0.27$}
		\caption{Projected density field $\bar{\gamma}$.}
		\label{fig:alexandersen_ubend_topology}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\validationplaceholder{Velocity-magnitude contours}
		\caption{Velocity magnitude field $u$.}
		\label{fig:alexandersen_ubend_velocity}
	\end{subfigure}
	\caption{FEniCS-SA optimization result for the U-bend benchmark at $V_f=0.27$. Final objective function value $J_p= [TBD]$.}
	\label{fig:alexandersen_ubend_fenics_results}
\end{figure}

For the matching $V_f=0.27$, $Re=5{,}000$ U-bend case, the conventional $k$-$\varepsilon$ result reported by Bayat et al.~\cite{Bayat2026} is $J_p=0.8327$. As for the pipe bend, this value provides a reference scale only; the comparison remains qualitative because the present solver uses a different turbulence model and near-wall treatment.

The U-bend convergence histories are collected in Figure~\ref{fig:alexandersen_ubend_histories}; the two panels separate the inlet-pressure objective history in Figure~\ref{fig:alexandersen_ubend_objective_history} from the enforced volume-fraction history in Figure~\ref{fig:alexandersen_ubend_volume_history}.

\begin{figure}[!ht]
	\centering
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\validationplaceholder{Objective function vs. optimization iteration}
		\caption{Objective function $J$ vs. MMA iteration.}
		\label{fig:alexandersen_ubend_objective_history}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\validationplaceholder{Volume constraint vs. optimization iteration}
		\caption{Volume fraction vs. MMA iteration.}
		\label{fig:alexandersen_ubend_volume_history}
	\end{subfigure}
	\caption{Optimization convergence histories for the U-bend benchmark.}
	\label{fig:alexandersen_ubend_histories}
\end{figure}

\subsection{CFD Validation}
\label{subsec:alexandersen_ubend_cfd}
The extracted U-bend topology is validated with the same sharp-geometry workflow as the pipe bend. The Ansys-SA solve checks cross-code consistency for the same turbulence closure family, while the Ansys SST $k$-$\omega$ solve tests whether the design remains robust under a two-equation model that is more sensitive to separation and adverse pressure gradients. The resulting validation metrics are summarized in Table~\ref{tab:alexandersen_ubend_validation_metrics}.

\begin{table}[!ht]
	\centering
	\footnotesize
	\caption{CFD validation metrics for the extracted U-bend topology.}
	\label{tab:alexandersen_ubend_validation_metrics}
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\textwidth}{l c c c X}
		\hline
		\textbf{Solver} & \textbf{Model} & \textbf{$\Phi_D$ [W/m]} & \textbf{$\Delta p$ [Pa]} & \textbf{Comment} \\
		\hline
		FEniCS diffuse design & SA & [TBD] & [TBD] & Optimization state \\
		Ansys sharp geometry & SA & [TBD] & [TBD] & Cross-code comparison \\
		Ansys sharp geometry & SST $k$-$\omega$ & [TBD] & [TBD] & Turbulence-model sensitivity \\
		\hline
	\end{tabularx}
\end{table}

\subsection{Comparison with Laminar Design}
\label{subsec:alexandersen_ubend_laminar}
The laminar-design comparison is reported in Section~\ref{sec:app_C_alexandersen_ubend} of Appendix~\ref{app:C_laminar_turbulent_topology_comparison}. The laminar reference uses the same U-bend mesh, inlet and outlet ports, separator geometry, and volume fraction, but it is optimized at $Re=1$ without the SA turbulence model. The extracted laminar and turbulent layouts are then evaluated under the same target turbulent operating conditions. This comparison is expected to show whether the laminar optimizer creates an overly tight return path around the separator tip, while the turbulent optimizer allocates fluid volume to reduce separation and momentum loss through the $180^\circ$ turn.

\section{Conclusion}
\label{sec:ch6_conclusion}
This chapter centers the final thesis evidence on the pipe-bend and U-bend benchmarks from Bayat et al.~\cite{Bayat2026}. The sensitivity verification is performed on the same penalized wall-distance, topology-penalized SA, frozen-turbulence formulation used for the optimization; therefore, the coordinate finite-difference and Taylor checks directly test the derivative supplied to MMA. The pipe-bend and U-bend results then test the same framework on two distinct curved-flow layouts.

The independent CFD validation using Ansys Fluent is therefore interpreted as a sharp-geometry check of the optimized designs, not merely as another internal FEniCS result. Testing the extracted topologies with both SA and SST $k$-$\omega$ quantifies how much of the optimized performance survives a change in turbulence closure. Together with the laminar comparisons, this chapter supports the thesis argument that high-Reynolds turbulence physics must be included directly in the optimization loop when designing viable internal-flow topologies.
