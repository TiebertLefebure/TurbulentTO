\chapter{Turbulent Topology Optimization Results}
\label{ch:benchmark_results}

\providecommand{\validationplaceholder}[1]{%
	\fbox{\parbox[c][0.22\textheight][c]{0.88\linewidth}{\centering \textbf{Placeholder}\\[0.5em]#1}}%
}

\section{Introduction}
\label{sec:ch6_introduction}
Building upon the FEniCS-SA solver verified in Chapter~\ref{ch:sa_verification} and the topology optimization methodology developed in Chapter~\ref{ch:topology_optimization_methodology}, this chapter evaluates the turbulent topology optimization solver on the pipe-bend and U-bend internal-flow benchmarks from Bayat et al.~\cite{Bayat2026} and the diffuser benchmark from Yoon~\cite{Yoon2016}. Table~\ref{tab:ch6_benchmark_cases} gives the operating points and validation analyses. Each benchmark section then introduces only the geometry, objective, and interpretation needed for that case.

The optimized layouts are interpreted through projected density fields, velocity fields, and independent sharp-geometry CFD validation in Ansys Fluent. Appendix~\ref{app:C_topology_optimization_benchmarks} retains the supporting frozen-sensitivity verification, FEniCS convergence histories, numerical settings, and Ansys grid-independence studies, grouped by benchmark case.

\section{Benchmark Cases and Evaluation Metrics}
\label{sec:ch6_strategy_metrics}

The three benchmarks cover curved internal flows and a diffuser with a prescribed outlet contraction. The Bayat cases use reciprocal wall-distance penalization, topology-dependent SA and wall-distance reaction terms, an average inlet-pressure objective, and a frozen-turbulence adjoint. The diffuser uses the same frozen-SA optimization machinery, but it minimizes the dissipation objective $J_D$.

\begin{table}[!ht]
	\centering
	\footnotesize
	\caption{Benchmark cases evaluated in Chapter~\ref{ch:benchmark_results}. Detailed physical and numerical parameters are listed in Appendix~\ref{app:C_topology_optimization_benchmarks}.}
	\label{tab:ch6_benchmark_cases}
	\renewcommand{\arraystretch}{1.2}
		\begin{tabularx}{\textwidth}{l c c >{\raggedright\arraybackslash}X}
		\hline
		\textbf{Case} & \textbf{$V_f$} & \textbf{$Re$} & \textbf{Analyses reported} \\
		\hline
			Pipe bend~\cite{Bayat2026} & $0.25$ & $5{,}000$ & FEniCS optimization; Ansys-SA and Ansys-SST $k-\omega$ CFD validation; laminar-design comparison \\
			U-bend~\cite{Bayat2026} & $0.27$ & $5{,}000$ & FEniCS optimization; Ansys-SA and Ansys-SST $k-\omega$ CFD validation; laminar-design comparison \\
			Diffuser~\cite{Yoon2016} & $0.30$ & $3{,}000$ & Velocity-outlet baseline; pressure-outlet FEniCS optimization with $J_D$; Ansys-SA and Ansys-SST $k-\omega$ CFD validation; laminar-design comparison for the pressure-outlet case \\
		\hline
	\end{tabularx}
\end{table}

The pipe-bend and U-bend cases minimize the average inlet-pressure objective $J_p$ defined in Eq.~\ref{eq:inlet_pressure_objective}, which keeps their setup comparable with the Bayat benchmark objective. The diffuser case minimizes the dissipation objective $J_D$ defined in Eq.~\ref{eq:dissipation_objective}, matching the formulation used by Yoon~\cite{Yoon2016}. All extracted geometries are then evaluated as sharp-wall Ansys Fluent models, where the Brinkman penalty is removed and the validation metrics are recomputed on the exported flow domain.

The primary validation metric is the physical viscous dissipation integrated over the full exported CFD domain, including the non-design inlet, outlet, and fixed regions that are retained in the final geometry,
\begin{equation}
	\Phi_D = \int_{\Omega}
	\frac{1}{2} \mu_{\mathrm{eff}}
	\left(\nabla\mathbf{u}+\nabla\mathbf{u}^{T}\right):
	\left(\nabla\mathbf{u}+\nabla\mathbf{u}^{T}\right)
	\,d\Omega ,
	\label{eq:ch6_validation_dissipation}
\end{equation}
where $\Omega$ denotes the complete sharp-geometry flow domain exported to Ansys and $\mathbf{S}=\frac{1}{2}\left(\nabla\mathbf{u}+\nabla\mathbf{u}^{T}\right)$ is the symmetric strain-rate tensor. The integrand in Eq.~\ref{eq:ch6_validation_dissipation} can therefore also be written as $2\mu_{\mathrm{eff}}\mathbf{S}:\mathbf{S}$. In the Ansys Fluent post-processing, the scalar strain-rate magnitude is $\dot{\gamma}=\sqrt{2\mathbf{S}:\mathbf{S}}$, so the custom field function $\mu_{\mathrm{eff}}\dot{\gamma}^{2}$ is equivalent to the integrand in Eq.~\ref{eq:ch6_validation_dissipation}. Because the reported Ansys validations all re-solve the extracted geometries as turbulent flows, the effective viscosity used in these post-processed metrics is $\mu_{\mathrm{eff}}=\mu+\mu_t$.

The second global validation metric is the static pressure drop between the physical inlet and outlet sections,
\begin{equation}
	\Delta p =
	\bar{p}_{\mathrm{in}}-\bar{p}_{\mathrm{out}},
	\qquad
	\bar{p}_{\Gamma} =
	\frac{1}{|\Gamma|}
	\int_{\Gamma} p_s\,d\Gamma ,
	\label{eq:ch6_static_pressure_drop}
\end{equation}
where $p_s$ is the static pressure and $|\Gamma|=\int_{\Gamma}1\,d\Gamma$ is the boundary length in the two-dimensional model. In Ansys Fluent, $p_s$ is taken directly from the area-weighted average static pressure on the inlet and outlet surfaces. For FEniCS results that use kinematic pressure, the corresponding static pressure is obtained as $p_s=\rho p_k$ before evaluating Eq.~\ref{eq:ch6_static_pressure_drop}. Pressure drop is retained throughout Chapter~\ref{ch:benchmark_results} because it is the most direct engineering interpretation of the same mechanical losses measured by $\Phi_D$. All two-dimensional dissipation quantities are evaluated per unit depth.

\section{Pipe Bend Benchmark}
\label{sec:alexandersen_pipebend}
The pipe-bend benchmark introduces a direct comparison with the turbulent topology optimization results of Bayat et al.~\cite{Bayat2026}. While the reference study employs a standard $k$-$\varepsilon$ RANS model with wall functions, the present framework tackles the same problem using a wall-resolved SA model under the frozen-turbulence assumption. This comparison tests whether the fundamental topological solution, a streamlined and separation-avoiding bend, remains consistent across different turbulence closures.

\subsection{Geometry and Boundary Conditions}
\label{subsec:alexandersen_pipebend_setup}
The boundary conditions and geometric constraints replicate the setup shown in Fig.~10 of Bayat et al.~\cite{Bayat2026}, specifically targeting the $V_f=0.25$ volume-fraction case. The inlet is placed on the left side of the non-design extension, the outlet is placed along the lower boundary, and the outlet pressure is fixed to zero. The optimization minimizes the average pressure on this prescribed inlet boundary required to drive the flow through the evolving bend.
The local benchmark geometry used for the present implementation is shown in Figure~\ref{fig:alexandersen_pipebend_setup}.

\begin{figure}[!ht]
	\centering
	\includegraphics[width=0.7\textwidth]{PipeBend-Geometry-BCs}
	\caption{Geometry and boundary conditions for the pipe-bend benchmark from Bayat et al.~\cite{Bayat2026}. The present evaluation targets the $V_f=0.25$ case using the SA model.}
	\label{fig:alexandersen_pipebend_setup}
\end{figure}

\subsection{Optimization Results}
\label{subsec:alexandersen_pipebend_fenics}
Figure~\ref{fig:alexandersen_pipebend_fenics_results} illustrates the projected density field $\bar{\gamma}$ and the corresponding velocity magnitude. The supporting coordinate finite-difference and Taylor checks for the frozen adjoint are reported in Appendix~\ref{app:C_topology_optimization_benchmarks}. The FEniCS-SA result shows the same broad routing strategy as the $k$-$\varepsilon$ reference from Bayat et al.~\cite{Bayat2026}: the main high-speed path bypasses the sharpest corners to reduce momentum loss. The exact gray-scale density distribution near the walls still differs because the turbulence closure and near-wall treatment are not the same.

\begin{figure}[!ht]
	\centering
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\includegraphics[width=\textwidth]{PipeBend-Turbulent-Topology-Jp}
		\caption{Projected density field $\bar{\gamma}$.}
		\label{fig:alexandersen_pipebend_topology}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.49\textwidth}
		\centering
		\includegraphics[width=\textwidth]{PipeBend-Turbulent-Velocity-FEniCS-Jp}
		\caption{Velocity magnitude and vector field.}
		\label{fig:alexandersen_pipebend_velocity}
	\end{subfigure}
	\caption{FEniCS-SA optimization result for the pipe-bend benchmark at $V_f=0.25$. Final objective value $J_p= [TBD]$ Pa.}
	\label{fig:alexandersen_pipebend_fenics_results}
\end{figure}

For the same $V_f=0.25$, $Re=5{,}000$ operating point, Bayat et al.~\cite{Bayat2026} report $J_p=0.4023$ Pa for their conventional $k$-$\varepsilon$ topology optimization. This value is used only as a reference scale, not as a direct validation target, because the turbulence closure and near-wall treatment differ from the present wall-resolved SA formulation.

The corresponding FEniCS convergence history is reported in Figure~\ref{fig:alexandersen_pipebend_histories} of Appendix~\ref{app:C_topology_optimization_benchmarks}. The objective decreases rapidly during the first part of the optimization, while later continuation updates mainly rebalance the pressure objective and active volume constraint.


\subsection{CFD Validation}
\label{subsec:alexandersen_pipebend_cfd}
The structural robustness of the extracted pipe-bend topology is verified in Table~\ref{tab:alexandersen_pipebend_validation_metrics}. Because the SST $k-\omega$ model is notably more sensitive to adverse pressure gradients than SA, evaluating the extracted geometry under both closures serves as a rigorous stress test for the optimized shape.

\begin{table}[!ht]
	\centering
	\footnotesize
	\caption{CFD validation metrics for the extracted pipe-bend topology.}
	\label{tab:alexandersen_pipebend_validation_metrics}
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\textwidth}{l c c c X}
		\hline
		\textbf{Solver} & \textbf{Model} & \textbf{$\Phi_D$ [W/m]} & \textbf{$\Delta p$ [Pa]} & \textbf{Comment} \\
		\hline
		FEniCS diffuse design & SA & [TBD] & [TBD] & Optimization state \\
		Ansys sharp geometry & SA & 0.0153 & 0.1457 & Cross-code comparison \\
		Ansys sharp geometry & SST $k-\omega$ & 0.0166 & 0.1522 & Turbulence-model sensitivity \\
		\hline
	\end{tabularx}
\end{table}

Using the Ansys-SA result as the reference, the SST $k-\omega$ evaluation increases the pressure drop by approximately $4.5\%$ and the viscous dissipation by approximately $8.5\%$. The SST $k-\omega$ model therefore predicts slightly higher losses, consistent with a closure that is more sensitive to adverse pressure gradients, but the changes remain modest. This suggests that the optimized pipe-bend geometry is robust under a change of turbulence closure, rather than being tuned only to the SA model.

\subsection{Comparison with Laminar Design}
\label{subsec:alexandersen_pipebend_laminar}
The laminar reference uses the same pipe-bend mesh, design-domain restrictions, inlet and outlet treatment, objective type, and volume fraction, but replaces the SA turbulent state equations by the laminar flow formulation and is optimized at $Re=1$. This laminar optimization favors a sharper, shorter turn that is appropriate for the creeping-flow reference problem but less suitable once the extracted geometry is operated at the target turbulent Reynolds number. The comparison is therefore interpreted under identical turbulent operating conditions, rather than by comparing the laminar and turbulent optimization objectives directly.

Figure~\ref{fig:alexandersen_pipebend_laminar_topologies} compares the laminar and turbulent optimized topologies before sharp-geometry extraction. The laminar-design mesh-independence study supporting the SST $k-\omega$ result is reported in Table~\ref{tab:app_C_alexandersen_pipebend_lam_mesh} of Appendix~\ref{app:C_topology_optimization_benchmarks}.

\begin{figure}[!ht]
	\centering
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\includegraphics[width=\textwidth]{PipeBend-Laminar-Topology-Jp}
		\caption{Projected density field $\bar{\gamma}$: laminar-optimized design ($Re=1$).}
		\label{fig:alexandersen_pipebend_laminar_topo}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\includegraphics[width=\textwidth]{PipeBend-Turbulent-Topology-Jp}
		\caption{Projected density field $\bar{\gamma}$: turbulent-optimized design ($Re=5{,}000$).}
		\label{fig:alexandersen_pipebend_turbulent_topo}
	\end{subfigure}
	\caption{Laminar and turbulent topology comparison for the pipe-bend benchmark.}
	\label{fig:alexandersen_pipebend_laminar_topologies}
\end{figure}


Figure~\ref{fig:alexandersen_pipebend_laminar_velocity_comparison} shows the corresponding SST $k-\omega$ velocity contours through the extracted laminar and turbulent pipe-bend designs. These contours provide the flow-field evidence behind the scalar metrics: sharper turns and corners in the laminar design promote stronger local separation and momentum loss when the geometry is operated at the turbulent Reynolds number.

\begin{figure}[!ht]
	\centering
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\includegraphics[width=\textwidth]{PipeBend-Laminar-VelocityMagnitude-SSTkomega}
		\caption{Velocity magnitude field $u$: laminar-optimized design.}
		\label{fig:alexandersen_pipebend_laminar_velocity_sst}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\includegraphics[width=\textwidth]{PipeBend-Turbulent-VelocityMagnitude-SSTkomega}
		\caption{Velocity magnitude field $u$: turbulent-optimized design.}
		\label{fig:alexandersen_pipebend_turbulent_velocity_sst}
	\end{subfigure}
	\caption{Velocity-contour comparison for the extracted pipe-bend designs under identical turbulent operating conditions (SST $k-\omega$ model).}
	\label{fig:alexandersen_pipebend_laminar_velocity_comparison}
\end{figure}

\begin{figure}[!ht]
	\centering
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\includegraphics[width=\textwidth]{PipeBend-Laminar-TurbViscRatio-SSTkomega}
		\caption{Turbulent viscosity ratio field $\mu_t/\mu$: laminar-optimized design.}
		\label{fig:alexandersen_pipebend_laminar_turbviscratio_sst}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\includegraphics[width=\textwidth]{PipeBend-Turbulent-TurbViscRatio-SSTkomega}
		\caption{Turbulent viscosity ratio field $\mu_t/\mu$: turbulent-optimized design.}
		\label{fig:alexandersen_pipebend_turbulent_turbviscratio_sst}
	\end{subfigure}
	\caption{Turbulent viscosity ratio comparison for the pipe-bend designs, evaluated with an identical turbulent flow (SST $k-\omega$ model).}
	\label{fig:alexandersen_pipebend_laminar_turbviscratio_comparison}
\end{figure}

The final SST $k-\omega$ comparison in Table~\ref{tab:alexandersen_pipebend_laminar_perf} evaluates the extracted laminar and turbulent pipe-bend designs under identical turbulent operating conditions. The turbulent design reduces the viscous dissipation from $0.0313$ to $0.0166~\mathrm{W/m}$ and the pressure drop from $0.5444$ to $0.1522~\mathrm{Pa}$. These reductions, $47.0\%$ and $72.0\%$ respectively, show that the turbulent topology optimization framework outperforms the laminar surrogate for the target turbulent operating point.

\begin{table}[!ht]
	\centering
	\footnotesize
	\caption{Performance comparison of the extracted pipe-bend topologies under identical turbulent operating conditions.}
	\label{tab:alexandersen_pipebend_laminar_perf}
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\textwidth}{X c c c X}
		\hline
		\textbf{Design origin} & \textbf{Model} & \textbf{$\Phi_D$ [W/m]} & \textbf{$\Delta p$ [Pa]} & \textbf{Relative performance} \\
		\hline
		Laminar TO, $Re=1$ & SST $k-\omega$ & $0.0313$ & $0.5444$ & Baseline \\
		Turbulent TO, SA & SST $k-\omega$ & $0.0166$ & $0.1522$ & $47.0\%$ lower $\Phi_D$; $72.0\%$ lower $\Delta p$ \\
		\hline
	\end{tabularx}
\end{table}

\section{U-Bend Benchmark}
\label{sec:alexandersen_ubend}
The U-bend is the companion benchmark to the pipe bend. It uses the same frozen-SA topology optimization framework, but the geometry forces a $180^\circ$ return path between two ports on the same side of the domain. This makes the case useful for checking whether the optimizer can form a smooth turbulent return duct without relying on the shorter $90^\circ$ routing available in the pipe-bend case.

\subsection{Geometry and Boundary Conditions}
\label{subsec:alexandersen_ubend_setup}
The geometry and boundary conditions follow Fig.~11 and Table~4 of Bayat et al.~\cite{Bayat2026}. The square design region has side length $L$, with a non-design lead duct of length $0.2L$ on the left. The inlet and outlet ports both have height $0.2L$; the top port acts as the inlet and the lower port acts as the pressure outlet. A fixed internal separator bar of thickness $0.10L$ extends from the lead duct into the design domain, forcing the flow to turn around its rounded tip. The target volume fraction is $V_f=0.27$, and the Reynolds number is $Re=5{,}000$ based on the port height and maximum inlet velocity.
Figure~\ref{fig:alexandersen_ubend_setup} shows the corresponding local geometry and boundary-condition layout used for this benchmark.

\begin{figure}[!ht]
	\centering
	\includegraphics[width=0.7\textwidth]{UBend-TO-Geometry-BCs}
	\caption{Geometry and boundary conditions for the U-bend benchmark from Bayat et al.~\cite{Bayat2026}.}
	\label{fig:alexandersen_ubend_setup}
\end{figure}

\subsection{Optimization Results}
\label{subsec:alexandersen_ubend_fenics}
Figure~\ref{fig:alexandersen_ubend_fenics_results} reports the final projected density and velocity-magnitude field for the U-bend. The main feature to assess is whether the design forms a rounded return passage around the separator tip, rather than a short path with tight local curvature. Because the objective is average inlet pressure, a successful layout reduces the pressure required to maintain the imposed inlet flow while respecting the prescribed volume fraction.

\begin{figure}[!ht]
	\centering
	\begin{subfigure}[b]{0.45\textwidth}
		\centering
		\includegraphics[width=\textwidth]{UBend-Turbulent-Topology-Jp}
		\caption{Projected density field $\bar{\gamma}$.}
		\label{fig:alexandersen_ubend_topology}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.53\textwidth}
		\centering
		\includegraphics[width=\textwidth]{UBend-Turbulent-Velocity-FEniCS-Jp}
		\caption{Velocity magnitude and vector field.}
		\label{fig:alexandersen_ubend_velocity}
	\end{subfigure}
	\caption{FEniCS-SA optimization result for the U-bend benchmark at $V_f=0.27$. Final objective value $J_p=TBD~\mathrm{Pa}$.}
	\label{fig:alexandersen_ubend_fenics_results}
\end{figure}

For the matching $V_f=0.27$, $Re=5{,}000$ U-bend case, the conventional $k$-$\varepsilon$ result reported by Bayat et al.~\cite{Bayat2026} is $J_p=0.8327$. As for the pipe bend, this value provides a reference scale only; the comparison remains qualitative because the present solver uses a different turbulence model and near-wall treatment.

The U-bend convergence history is reserved for Figure~\ref{fig:alexandersen_ubend_histories} of Appendix~\ref{app:C_topology_optimization_benchmarks}, using the same combined objective and volume-residual format as the pipe-bend diagnostic.

\subsection{CFD Validation}
\label{subsec:alexandersen_ubend_cfd}
The extracted U-bend topology is validated with the same sharp-geometry workflow as the pipe bend. The Ansys SST $k-\omega$ solve tests whether the design remains robust under a two-equation model that is more sensitive to separation and adverse pressure gradients, while the Ansys-SA solve checks cross-code consistency for the same turbulence closure family. The resulting validation metrics are summarized in Table~\ref{tab:alexandersen_ubend_validation_metrics}.

\begin{table}[!ht]
	\centering
	\footnotesize
	\caption{CFD validation metrics for the extracted U-bend topology.}
	\label{tab:alexandersen_ubend_validation_metrics}
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\textwidth}{l c c c X}
		\hline
		\textbf{Solver} & \textbf{Model} & \textbf{$\Phi_D$ [W/m]} & \textbf{$\Delta p$ [Pa]} & \textbf{Comment} \\
		\hline
		FEniCS diffuse design & SA & [TBD] & [TBD] & Optimization state \\
		Ansys sharp geometry & SA & [TBD] & [TBD] & Cross-code comparison \\
		Ansys sharp geometry & SST $k-\omega$ & [TBD] & [TBD] & Turbulence-model sensitivity \\
		\hline
	\end{tabularx}
\end{table}

\subsection{Comparison with Laminar Design}
\label{subsec:alexandersen_ubend_laminar}
The laminar reference uses the same U-bend mesh, inlet and outlet ports, separator geometry, objective type, and volume fraction, but it is optimized at $Re=1$ without the SA turbulence model. The extracted laminar and turbulent layouts are then evaluated under the same target turbulent operating conditions. This comparison is expected to show whether the laminar optimizer creates an overly tight return path around the separator tip, while the turbulent optimizer allocates fluid volume to reduce separation and momentum loss through the $180^\circ$ turn.

Figure~\ref{fig:alexandersen_ubend_laminar_topologies} compares the laminar and turbulent optimized U-bend topologies. The laminar-design mesh-independence study supporting the SST $k-\omega$ result is reported in Table~\ref{tab:app_C_alexandersen_ubend_lam_mesh} of Appendix~\ref{app:C_topology_optimization_benchmarks}.

\begin{figure}[!ht]
	\centering
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\includegraphics[width=\textwidth]{UBend-Laminar-Topology-Jp}
		\caption{Projected density field $\bar{\gamma}$: laminar-optimized design ($Re=1$). Final objective value $J_p=143.254~\mathrm{Pa}$.}
		\label{fig:alexandersen_ubend_laminar_topo}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\includegraphics[width=\textwidth]{UBend-Turbulent-Topology-Jp}
		\caption{Projected density field $\bar{\gamma}$: turbulent-optimized design ($Re=5{,}000$). Final objective value $J_p=[TBD]~\mathrm{Pa}$.}
		\label{fig:alexandersen_ubend_turbulent_topo}
	\end{subfigure}
	\caption{Laminar and turbulent topology comparison for the U-bend benchmark.}
	\label{fig:alexandersen_ubend_laminar_topologies}
\end{figure}

Figure~\ref{fig:alexandersen_ubend_laminar_velocity_comparison} shows the SST $k-\omega$ velocity contours for the extracted U-bend designs. In this case the comparison is especially useful near the separator tip, where a laminar-optimized shortcut can create a tighter return path, stronger adverse-pressure-gradient effects, and larger separated or low-momentum regions than the turbulent design.

\begin{figure}[!ht]
	\centering
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\includegraphics[width=\textwidth]{UBend-Laminar-Jp-Velocity-Magnitude-SSTkomega}
		\caption{Velocity magnitude field $u$: laminar-optimized design.}
		\label{fig:alexandersen_ubend_laminar_velocity_sst}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\validationplaceholder{SST $k-\omega$ velocity contour through turbulent U-bend design}
		\caption{Velocity magnitude field $u$: turbulent-optimized design.}
		\label{fig:alexandersen_ubend_turbulent_velocity_sst}
	\end{subfigure}
	\caption{SST $k-\omega$ velocity-contour comparison for the extracted U-bend designs under identical turbulent operating conditions.}
	\label{fig:alexandersen_ubend_laminar_velocity_comparison}
\end{figure}

Table~\ref{tab:alexandersen_ubend_laminar_perf} compares the extracted laminar and turbulent U-bend topologies under identical turbulent operating conditions. If the turbulent design lowers $\Delta p$ and $\Phi_D$ under the SST $k-\omega$ evaluation, the result supports the thesis claim that high-Reynolds topology optimization should include turbulence directly in the design loop.

\begin{table}[!ht]
	\centering
	\footnotesize
	\caption{Performance comparison of the extracted U-bend topologies under identical turbulent operating conditions.}
	\label{tab:alexandersen_ubend_laminar_perf}
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\textwidth}{X c c c X}
		\hline
		\textbf{Design origin} & \textbf{Model} & \textbf{$\Phi_D$ [W/m]} & \textbf{$\Delta p$ [Pa]} & \textbf{Relative performance} \\
		\hline
		Laminar TO, $Re=1$ & SST $k-\omega$ & $0.107$ & $1.150$ & Baseline \\
		Turbulent TO, SA & SST $k-\omega$ & [TBD] & [TBD] & [TBD]\% vs. laminar \\
		\hline
	\end{tabularx}
\end{table}

\section{Diffuser Benchmark}
\label{sec:yoon_diffuser}
The diffuser benchmark follows the third numerical example of Yoon~\cite{Yoon2016}. It is used here to test two sensitivities that do not appear in the bend cases: the downstream boundary condition and the turbulence closure used after geometry extraction. A velocity-outlet baseline follows the published setup, while the primary result replaces the prescribed outlet velocity profile with a pressure outlet so that the outlet velocity distribution is solved rather than imposed.

\subsection{Velocity-Outlet Boundary-Condition Sensitivity}
\label{subsec:yoon_diffuser_velocity_outlet}
The velocity-outlet baseline prescribes parabolic velocity profiles at both the full-height inlet and the center-third outlet. It produces a more strongly diverging channel near the outlet, which is interpreted as partly boundary-condition-driven. Its geometry, FEniCS fields, and Ansys diagnostic contour are reported in Appendix~\ref{app:C_topology_optimization_benchmarks}; the pressure-outlet variant is used as the primary diffuser result below.

\subsection{Pressure-Outlet Setup and Optimization Results}
\label{subsec:yoon_diffuser_pressure_setup}
\label{subsec:yoon_diffuser_pressure_fenics}
The pressure-outlet variant changes only the downstream boundary treatment relative to the velocity-outlet baseline documented in Appendix~\ref{app:C_topology_optimization_benchmarks}. Its local geometry and boundary-condition layout are shown in Figure~\ref{fig:yoon_diffuser_pressure_setup}. The inlet remains the shifted parabolic velocity profile with $U_{\max,\mathrm{in}}=3~\mathrm{m/s}$ and the same $Re=3{,}000$ definition, while the center outlet is assigned a fixed static pressure $p=0~\mathrm{Pa}$. No outlet velocity profile is prescribed. This makes the outlet mass flux and velocity distribution part of the solution and removes the direct Dirichlet forcing that affected the velocity-outlet topology.

\begin{figure}[!ht]
	\centering
	\includegraphics[width=0.8\textwidth]{Diffuser-Geometry-BCs-PreOut}
	\caption{Geometry and boundary conditions for the pressure-outlet diffuser variant. The full-height inlet retains the shifted parabolic velocity profile, while the center-third outlet is assigned a fixed static pressure $p=0~\mathrm{Pa}$ instead of a prescribed velocity profile.}
	\label{fig:yoon_diffuser_pressure_setup}
\end{figure}

Figure~\ref{fig:yoon_diffuser_pressure_fenics_results} reports the pressure-outlet FEniCS-SA topology and velocity field. Compared with the velocity-outlet result in Appendix~\ref{app:C_topology_optimization_benchmarks}, the outlet region is less strongly widened. The optimized pressure-outlet objective is also lower, decreasing from $J_D=3.1164\times10^5~\mathrm{W/m}$ to $J_D=2.7664\times10^5~\mathrm{W/m}$, or by about $11.2\%$. This is not a direct performance ranking between equivalent designs, because the boundary-value problem has changed. The conclusion is narrower: removing the outlet Dirichlet velocity profile reduces the amount of geometry change required near the outlet and gives a less forced diffuser design.

\begin{figure}[!ht]
	\centering
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\includegraphics[width=\textwidth]{Diffuser-Turbulent-Topology-PreOut}
		\caption{Projected density field $\bar{\gamma}$.}
		\label{fig:yoon_diffuser_pressure_topology}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.50\textwidth}
		\centering
		\includegraphics[width=\textwidth]{Diffuser-Turbulent-Velocity-FEniCS-PreOut}
		\caption{Velocity magnitude and vector field.}
		\label{fig:yoon_diffuser_pressure_velocity}
	\end{subfigure}
	\caption{FEniCS-SA optimization result for the pressure-outlet diffuser at $V_f=0.30$. Final objective value $J_D=2.7664\times10^5~\mathrm{W/m}$.}
	\label{fig:yoon_diffuser_pressure_fenics_results}
\end{figure}

The corresponding pressure-outlet convergence history through this common comparison point is reported in Figure~\ref{fig:yoon_diffuser_pressure_convergence} of Appendix~\ref{app:C_topology_optimization_benchmarks}.

\subsection{Pressure-Outlet CFD Validation}
\label{subsec:yoon_diffuser_pressure_cfd}
The extracted pressure-outlet topology is validated with the same sharp-geometry workflow as the pipe bend and U-bend. The Ansys-SA solve checks the transfer from the diffuse FEniCS design to a sharp-wall geometry under the same closure family, while the Ansys SST $k-\omega$ solve tests whether the ranking is robust under an independent two-equation model. Because the optimization objective is $J_D$, the validation emphasizes both physical viscous dissipation and static pressure drop.

\begin{table}[!ht]
	\centering
	\footnotesize
	\caption{CFD validation metrics for the extracted pressure-outlet diffuser topology.}
	\label{tab:yoon_diffuser_validation_metrics}
	\renewcommand{\arraystretch}{1.2}
		\begin{tabularx}{\textwidth}{l c c >{\raggedright\arraybackslash}X}
		\hline
		\textbf{Evaluation} & \textbf{$\Phi_D$ [W/m]} & \textbf{$\Delta p_s$ [Pa]} & \textbf{Interpretation} \\
		\hline
		FEniCS-SA diffuse topology & $2.6307\times10^5$ & $2.6022\times10^5$ & Diffuse-domain diagnostic; total objective $J_D=2.7664\times10^5~\mathrm{W/m}$ \\
		Ansys-SA sharp geometry & $1.1885\times10^5$ & $1.1541\times10^5$ & Sharp-wall validation with the optimization closure family \\
		Ansys-SST $k-\omega$ sharp geometry & $1.2834\times10^4$ & $5.0006\times10^4$ & Independent closure check; much lower modeled dissipation \\
		\hline
	\end{tabularx}
\end{table}

The FEniCS-SA values in Table~\ref{tab:yoon_diffuser_validation_metrics} are approximately $2.21$ times larger in $\Phi_D$ and $2.25$ times larger in $\Delta p_s$ than the Ansys-SA sharp-geometry values. This discrepancy is too large to claim quantitative cross-code agreement. It is interpreted as a transfer limitation of this diffuser case: the FEniCS result is a diffuse Brinkman-domain optimization diagnostic, whereas the Ansys result is a sharp-wall validation of an extracted boundary. The mismatch is amplified by the pressure-outlet diffuser's separated or recovering downstream flow. The final hydraulic performance is therefore judged from the Ansys validation solves, while the FEniCS values document the optimization objective reached by the design loop.

The Ansys-SA loss is also substantially larger than the SST $k-\omega$ loss. For the turbulent pressure-outlet design, the SA pressure drop is $2.31$ times the SST $k-\omega$ value, and the SA dissipation is $9.26$ times the SST $k-\omega$ value. The velocity-magnitude fields in Figure~\ref{fig:yoon_diffuser_pressure_closure_diagnostics} are qualitatively similar, so the split cannot be explained by a clearly different bulk flow path. It is instead interpreted from the scalar metrics as turbulence-closure sensitivity in a separated or recovering diffuser flow. Since the validation dissipation integrand is $2\mu_{\mathrm{eff}}\mathbf{S}:\mathbf{S}$ through Eq.~\ref{eq:ch6_validation_dissipation}, differences in modeled eddy viscosity can strongly affect the integrated dissipation even when velocity contours look similar. Pointwise turbulent-viscosity-ratio contours were therefore not used as the main evidence for this conclusion, because localized spikes and interpolation-sensitive patches can be overinterpreted visually.

\begin{figure}[!htbp]
	\centering
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\includegraphics[width=\textwidth]{Diffuser-Turbulent-VelMagn-PreOut-SA}
		\caption{Ansys-SA velocity magnitude.}
		\label{fig:yoon_diffuser_pressure_velocity_sa}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.47\textwidth}
		\centering
		\includegraphics[width=\textwidth]{Diffuser-Turbulent-VelMagn-PreOut-SSTkomega}
		\caption{Ansys-SST $k-\omega$ velocity magnitude.}
		\label{fig:yoon_diffuser_pressure_velocity_sst}
	\end{subfigure}
	\caption{Velocity diagnostics for the pressure-outlet turbulent diffuser.}
	\label{fig:yoon_diffuser_pressure_closure_diagnostics}
\end{figure}

Local dissipation-density contours were also inspected, but they are not retained as a main figure because the field is dominated by the inlet contraction edges and does not clearly identify the downstream separation or recovery behavior. This is physically consistent with the form of the dissipation density, $2\mu_{\mathrm{eff}}\mathbf{S}:\mathbf{S}$: it peaks where strain rates are largest, which in these diffuser designs occurs mainly where the full-height inlet flow is accelerated into the narrow channel. The widened or recovering downstream region can still be important for outlet sensitivity and turbulence-model dependence, but a separated low-speed region does not necessarily create a large local dissipation-density peak if the local strain rate is modest. For this diffuser, the main visual diagnostics are therefore the topology and velocity field, while integrated dissipation and pressure drop remain the scalar validation metrics.

\subsection{Pressure-Outlet Laminar-Design Comparison}
\label{subsec:yoon_diffuser_pressure_laminar}
The laminar reference for the final diffuser comparison uses the same pressure-outlet boundary treatment, objective type, and volume fraction, but it is optimized at $Re=1$ without the SA turbulence model. The extracted laminar and turbulent pressure-outlet layouts are then evaluated under the same target turbulent operating conditions. This comparison isolates whether the SA-optimized pressure-outlet design remains beneficial when the final sharp geometry is re-solved with the same closure and with SST $k-\omega$.

Table~\ref{tab:yoon_diffuser_pressure_laminar_perf} compares the extracted laminar and turbulent pressure-outlet diffuser topologies under identical turbulent operating conditions. The topology and velocity fields were inspected, but they do not by themselves reveal the performance ranking clearly enough to justify a separate figure. Because the diffuser is optimized with $J_D$, lower $\Phi_D$ is the primary performance indicator, while the static pressure drop remains the most direct hydraulic interpretation of the extracted sharp geometry. Both SA and SST $k-\omega$ validation solves are included because the diffuser benchmark is expected to remain turbulence-model-sensitive when separation is present.

\begin{table}[!ht]
	\centering
	\footnotesize
	\caption{Performance comparison of the extracted pressure-outlet diffuser topologies under identical turbulent operating conditions.}
	\label{tab:yoon_diffuser_pressure_laminar_perf}
	\renewcommand{\arraystretch}{1.2}
		\begin{tabularx}{\textwidth}{l c c >{\raggedright\arraybackslash}X}
		\hline
		\textbf{Evaluation} & \textbf{$\Phi_D$ [W/m]} & \textbf{$\Delta p_s$ [Pa]} & \textbf{Relative performance} \\
		\hline
		Laminar TO, Ansys-SA & $1.3495\times10^5$ & $1.3979\times10^5$ & Baseline under SA \\
		Turbulent TO, Ansys-SA & $1.1885\times10^5$ & $1.1541\times10^5$ & $11.9\%$ lower $\Phi_D$; $17.4\%$ lower $\Delta p_s$ \\
		Laminar TO, Ansys-SST $k-\omega$ & $1.1289\times10^4$ & $3.8332\times10^4$ & Baseline under SST $k-\omega$ \\
		Turbulent TO, Ansys-SST $k-\omega$ & $1.2834\times10^4$ & $5.0006\times10^4$ & $13.7\%$ higher $\Phi_D$; $30.5\%$ higher $\Delta p_s$ \\
		\hline
	\end{tabularx}
\end{table}

The SA validation gives the positive turbulent-TO result sought in this benchmark: the SA-optimized turbulent topology reduces the pressure drop by $17.4\%$ and the viscous dissipation by $11.9\%$ relative to the pressure-outlet laminar topology. Under SST $k-\omega$, however, the ranking reverses. The turbulent topology gives a $30.5\%$ higher pressure drop and a $13.7\%$ higher dissipation than the laminar topology. This is not reported as a failure of the SA topology optimizer, because the optimizer did improve the design under the closure used in the design loop. It is instead reported as a closure-sensitive diffuser result: in this separated pressure-outlet case, the design ranking depends on whether the final sharp geometry is evaluated with SA or SST $k-\omega$.

\section{Conclusion}
\label{sec:ch6_conclusion}
This chapter answers the third research question by evaluating whether the turbulent topology optimization framework produces designs that remain credible after independent CFD validation. The pipe-bend result provides the strongest completed evidence: the Appendix~\ref{app:C_topology_optimization_benchmarks} frozen-sensitivity checks confirm the derivative supplied to MMA for $J_p$, and the extracted design remains robust when validated in Ansys Fluent with both SA and SST $k-\omega$.

For the pipe bend, changing the Ansys closure from SA to SST $k-\omega$ increases the validated pressure drop by only about $4.5\%$ and the viscous dissipation by about $8.5\%$, indicating that the optimized geometry is not narrowly tuned to the SA model. Under identical SST $k-\omega$ operating conditions, the turbulent design also reduces the pressure drop and viscous dissipation relative to the laminar-optimized design, supporting the claim that high-Reynolds turbulence physics should be included directly in the design loop. The diffuser is treated more cautiously. The velocity-outlet baseline shows that a prescribed outlet profile can influence the optimized outlet geometry, so the pressure-outlet variant is used as the primary diffuser result. For this pressure-outlet diffuser, the turbulent SA design improves on the laminar design under Ansys-SA, reducing $\Delta p_s$ by $17.4\%$ and $\Phi_D$ by $11.9\%$. The same two geometries evaluated with SST $k-\omega$ give the opposite ranking, with the turbulent design increasing $\Delta p_s$ by $30.5\%$ and $\Phi_D$ by $13.7\%$. The diffuser therefore supports the value of turbulence-aware TO within the SA design model, but it does not support a closure-independent performance claim. It is retained as a closure-sensitivity result for separated diffuser flow. The U-bend remains the main incomplete high-Reynolds benchmark and should be judged by the same validation standard once its remaining metrics are replaced.
