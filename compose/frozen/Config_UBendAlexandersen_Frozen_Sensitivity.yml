name: ttofrozen-ubend-alexandersen-sensitivity
services:
  run:
    container_name: turbulentto-frozen-ubend-alexandersen-sensitivity
    build:
      context: .
      dockerfile: Dockerfile
    image: turbulentto-frozen-fenics:local
    platform: linux/amd64
    working_dir: /home/fenics/shared
    volumes:
      - ../../:/home/fenics/shared
    environment:
      HDF5_USE_FILE_LOCKING: "FALSE"
    command:
      - FluidTO/TurbulentTO_Frozen.py
      - --config
      - FluidTO/Configs_Frozen/Config_UBendAlexandersen_Frozen_Sensitivity.py
