# Zelfde FEniCS-stack als Tijbert (quay), maar zonder Phusion my_init als PID 1 —
# dat script crasht op Python 3 bij afsluiten (os.system TypeError).
FROM quay.io/fenicsproject/stable:current
ENTRYPOINT ["python3"]
